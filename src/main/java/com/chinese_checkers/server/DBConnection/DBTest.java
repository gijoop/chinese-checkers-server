package com.chinese_checkers.server.DBConnection;

public class DBTest {
    public static void main(String[] args) {
        // AnnotationConfigApplicationContext context = 
        //         new AnnotationConfigApplicationContext(SpringConfig.class);

        // GameRepository gameRepository = new GameRepository(context.getBean("jdbcTemplate", JdbcTemplate.class));
        // MoveRepository moveRepository = new MoveRepository(context.getBean("jdbcTemplate", JdbcTemplate.class));

        
        // Move move = new Move(1, 5, 1, 1, 0, 0, 1, 1);
        // moveRepository.save(move);

        // context.close();
    }
}